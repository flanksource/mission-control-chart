{{- if .Values.upstream.createSecret }}
{{- $_ := required "upstream.host is a required value" .Values.upstream.host }}
{{- $_ := required "upstream.password is a required value" .Values.upstream.password }}
{{- $_ := required "upstream.agentName is a required value" .Values.upstream.agentName }}
{{- end }}
