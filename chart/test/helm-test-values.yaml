authProvider: basic
config-db:
  logLevel: -vvv
global:
  ui:
    host: mission-control.cluster.local
htpasswd:
  create: true
ingress:
  annotations:
    cert-manager.io/cluster-issuer: self-signed
  enabled: true
logLevel: -vvv
